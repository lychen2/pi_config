{
  "name": "pi-deferred-tools",
  "version": "0.2.0",
  "description": "Project-scoped TUI selector for Pi extension tools (legacy package name; tools are not deferred)",
  "license": "MIT",
  "type": "module",
  "keywords": ["pi-package", "pi", "tool-selector", "tui"],
  "files": ["extensions", "README.md", "config.example.json", "LICENSE"],
  "pi": { "extensions": ["./extensions/deferred-tools.ts"] },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.82.0",
    "@earendil-works/pi-tui": "^0.82.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
