{
  "name": "pi-todo-guard",
  "version": "0.1.0",
  "description": "Continue settled Pi runs while compatible Todo tasks remain open",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=22.19.0" },
  "keywords": ["pi-package", "pi", "todo", "autonomy", "workflow"],
  "files": ["index.ts", "README.md", "LICENSE"],
  "pi": { "extensions": ["./index.ts"] },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": { "@earendil-works/pi-coding-agent": ">=0.85.0 <0.86.0" },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2"
  }
}
